const express = require('express');
const router = express.Router();

// Handle order submissions (POST /orders)
router.post('/orders', async (req, res) => {
    const orderData = req.body;

    

});

module.exports = router;